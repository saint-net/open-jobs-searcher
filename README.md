# 🔍 Open Jobs Searcher

Инструмент для поиска вакансий с различных платформ.

## Возможности

- 🌐 Поиск вакансий на HeadHunter (hh.ru) - Россия
- 🇩🇪 Поиск вакансий на StepStone.de - Германия
- 🇦🇹 Поиск вакансий на Karriere.at - Австрия
- 🤖 Поиск вакансий на любом сайте компании с помощью LLM
- 🎯 Автоматическое определение популярных job board платформ (Personio, Greenhouse, Lever, Workable, Recruitee, HiBob, Deloitte, SmartRecruiters, Ashby, Breezy и др.)
- 📊 Гибридная экстракция вакансий (Schema.org + LLM)
- 📑 Автоматическая пагинация (до 3 страниц)
- 💾 Кэширование вакансий в SQLite с отслеживанием изменений
- 🧠 LLM кэширование ответов для экономии токенов (до 70% экономии)
- 📜 История изменений: новые/закрытые/вернувшиеся вакансии
- 🎯 Фильтрация по городу, опыту, зарплате
- 📊 Экспорт результатов в JSON и CSV
- 🖥️ Красивый вывод в терминале

## Установка

```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/macOS)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### Для поиска по сайтам (LLM)

По умолчанию используется **OpenRouter** с моделью `openai/gpt-4o-mini`.

Создайте файл `.env` с API ключом:

```bash
OPENROUTER_API_KEY=your-api-key-here
# Опционально: выбор конкретного провайдера OpenRouter
OPENROUTER_PROVIDER=chutes
```

Получить ключ можно на [openrouter.ai](https://openrouter.ai/)

#### Доступные OpenRouter провайдеры

Для модели `openai/gpt-4o-mini` OpenRouter автоматически выбирает лучших провайдеров.
Для альтернативных моделей (например `openai/gpt-oss-120b`) можно указать конкретный провайдер через `--openrouter-provider`.

#### Альтернатива: Ollama (локально)

Установите [Ollama](https://ollama.ai/) и скачайте модель:

```bash
ollama pull gpt-oss:20b
```

### Для SPA сайтов (Playwright)

```bash
playwright install chromium
```

## Использование

### Поиск на HeadHunter (Россия)

```bash
# Базовый поиск
python main.py search "Python Developer"

# Поиск с указанием города
python main.py search "Data Scientist" --location "Санкт-Петербург"

# Поиск с минимальной зарплатой
python main.py search "Frontend Developer" --salary 150000

# Поиск с фильтром по опыту
python main.py search "DevOps" --experience "1-3"
```

### Поиск на StepStone.de (Германия)

```bash
# Базовый поиск
python main.py stepstone "Python Developer"

# Поиск с указанием города
python main.py stepstone "Data Scientist" --location "Berlin"

# Сохранение результатов
python main.py stepstone "Frontend Developer" --location "Munich" --output jobs.json
```

### Поиск на Karriere.at (Австрия)

```bash
# Базовый поиск
python main.py karriere "Python Developer"

# Поиск с указанием города
python main.py karriere "Data Scientist" --location "Wien"

# Сохранение результатов
python main.py karriere "Frontend Developer" --location "Graz" --output jobs.json
```

### 🆕 Поиск на сайте компании (LLM)

```bash
# Поиск вакансий на сайте компании (OpenRouter по умолчанию)
python main.py website https://example.com

# Использовать конкретный OpenRouter провайдер
python main.py website https://example.com --openrouter-provider siliconflow

# Использовать локальную Ollama
python main.py website https://company.ru --provider ollama

# Без браузера (быстрее, но не работает с SPA)
python main.py website https://company.ru --no-browser

# Без кэширования в БД (одноразовый поиск)
python main.py website https://company.ru --nodb

# Без отладочной информации
python main.py website https://company.ru --no-verbose

# Сохранить результаты
python main.py website https://company.ru --output jobs.json
```

### 🔗 Поиск URL вакансий (NEW)

```bash
# Найти все URL вакансий на странице карьеры
python main.py find-job-urls https://company.com/careers

# С выбором провайдера
python main.py find-job-urls https://company.com/jobs --provider ollama
```

### 📜 История изменений

```bash
# Показать историю всех сайтов
python main.py history

# История конкретного сайта
python main.py history company.com

# Ограничить количество записей
python main.py history --limit 20
```

### 📋 Кэшированные сайты

```bash
# Показать все кэшированные сайты
python main.py sites
```

### Сохранение результатов

```bash
# Сохранить в JSON
python main.py search "Python" --output results.json

# Сохранить в CSV
python main.py search "Python" --output results.csv --format csv
```

## Команды

### `search` - Поиск на HeadHunter (Россия)

| Опция | Описание |
|-------|----------|
| `--location, -l` | Город/локация |
| `--experience, -e` | Опыт: `no_experience`, `1-3`, `3-6`, `6+` |
| `--salary, -s` | Минимальная зарплата |
| `--output, -o` | Путь для сохранения результатов |
| `--format, -f` | Формат: `json` или `csv` |
| `--limit, -n` | Количество результатов (по умолчанию 20) |

### `stepstone` - Поиск на StepStone.de (Германия)

| Опция | Описание |
|-------|----------|
| `--location, -l` | Город в Германии (например, Berlin, Munich, Frankfurt) |
| `--output, -o` | Путь для сохранения результатов |
| `--format, -f` | Формат: `json` или `csv` |
| `--page, -p` | Номер страницы (начиная с 0) |

### `karriere` - Поиск на Karriere.at (Австрия)

| Опция | Описание |
|-------|----------|
| `--location, -l` | Город в Австрии (например, Wien, Graz, Salzburg) |
| `--output, -o` | Путь для сохранения результатов |
| `--format, -f` | Формат: `json` или `csv` |
| `--page, -p` | Номер страницы (начиная с 0) |

### `website` - Поиск на сайте компании

| Опция | Описание |
|-------|----------|
| `--browser, -b` | Использовать браузер Playwright (по умолчанию включен) |
| `--provider, -p` | LLM провайдер: `openrouter` (по умолчанию), `ollama` |
| `--model, -m` | Модель LLM (по умолчанию `openai/gpt-4o-mini` для openrouter, `gpt-oss:20b` для ollama) |
| `--openrouter-provider` | Бэкенд-провайдер OpenRouter: `chutes`, `siliconflow`, `novitaai`, `gmicloud`, `deepinfra` |
| `--output, -o` | Путь для сохранения результатов |
| `--format, -f` | Формат: `json` или `csv` |
| `--verbose, -v` | Показать отладочную информацию (по умолчанию включен) |
| `--nodb` | Не использовать базу данных (без кэширования и истории) |

### `find-job-urls` - Поиск URL вакансий на странице

| Опция | Описание |
|-------|----------|
| `--provider, -p` | LLM провайдер: `openrouter` (по умолчанию), `ollama` |
| `--model, -m` | Модель LLM |
| `--openrouter-provider` | Бэкенд-провайдер OpenRouter |
| `--verbose, -v` | Показать отладочную информацию |

### `history` - История изменений вакансий

| Опция | Описание |
|-------|----------|
| `domain` | Домен сайта для фильтрации (опционально) |
| `--limit, -n` | Количество записей (по умолчанию 50) |

### `sites` - Кэшированные сайты

Показывает все сайты в базе данных с информацией о вакансиях и последнем сканировании.

## Структура проекта

```
open-jobs-searcher/
├── main.py              # CLI интерфейс
├── requirements.txt     # Зависимости
├── pyproject.toml       # Конфигурация проекта
├── data/                # Данные (SQLite база)
│   └── jobs.db          # Кэш вакансий и история
├── src/
│   ├── config.py        # Настройки приложения
│   ├── constants.py     # Глобальные константы
│   ├── models.py        # Модели данных
│   ├── output.py        # Вывод результатов
│   ├── database/        # 💾 SQLite кэширование
│   │   ├── connection.py    # Подключение и схема БД
│   │   ├── models.py        # Модели данных БД
│   │   └── repository.py    # CRUD операции
│   ├── extraction/      # 🔍 Гибридная экстракция
│   │   ├── extractor.py     # HybridJobExtractor
│   │   ├── candidate.py     # JobCandidate с scoring
│   │   └── strategies.py    # Schema.org + PDF стратегии
│   ├── browser/         # Playwright загрузчик (SPA)
│   │   ├── loader.py        # Загрузчик страниц (lazy loading, scrolling)
│   │   ├── navigation.py    # Навигация по сайту
│   │   ├── cookie_handler.py # Обработка cookies
│   │   ├── patterns.py      # Паттерны для поиска
│   │   └── exceptions.py    # Исключения
│   ├── llm/             # LLM провайдеры
│   │   ├── base.py          # Базовый класс LLM
│   │   ├── cache.py         # LLM response cache (namespace-based TTL)
│   │   ├── html_utils.py    # Утилиты для HTML/JSON, HTML→Markdown
│   │   ├── job_extraction.py # LLMJobExtractor
│   │   ├── url_discovery.py # LLMUrlDiscovery
│   │   ├── openrouter.py    # OpenRouter провайдер (structured output)
│   │   ├── ollama.py        # Ollama провайдер
│   │   └── prompts.py       # Промпты для парсинга
│   └── searchers/
│       ├── base.py           # Базовый класс поисковика
│       ├── hh.py             # HeadHunter API
│       ├── stepstone.py      # StepStone.de
│       ├── karriere.py       # Karriere.at
│       ├── website.py        # Универсальный парсер (оркестратор)
│       ├── page_fetcher.py   # HTTP/Browser загрузка страниц
│       ├── job_converter.py  # Конвертация в Job модели
│       ├── company_info.py   # Извлечение info о компании
│       ├── cache_manager.py  # CacheManager (кэширование)
│       ├── job_extraction.py # JobExtractor (пагинация)
│       ├── job_filters.py    # Фильтры и нормализация
│       ├── http_client.py    # HTTP клиент с retry
│       ├── url_discovery.py  # Поиск careers страниц
│       └── job_boards/       # Парсеры job-платформ
│           ├── base.py       # Базовый класс парсера
│           ├── detector.py   # Детекция платформы
│           ├── registry.py   # Реестр парсеров
│           ├── personio.py   # Personio
│           ├── greenhouse.py # Greenhouse
│           ├── lever.py      # Lever
│           ├── workable.py   # Workable
│           ├── recruitee.py  # Recruitee
│           ├── hrworks.py    # HRworks
│           ├── hibob.py      # HiBob
│           ├── odoo.py       # Odoo CMS
│           └── deloitte.py   # Deloitte
└── README.md
```

## Поддерживаемые платформы

### Job Boards (автоматическое определение)

Инструмент автоматически определяет и парсит следующие платформы:
- **Personio** - популярная HR платформа в Европе
- **Greenhouse** - ATS система для найма
- **Lever** - платформа для рекрутинга
- **Workable** - система управления наймом
- **Recruitee** - платформа для найма (API-based)
- **HiBob** - HR платформа (Angular SPA)
- **HRworks** - HR и ATS платформа
- **Odoo** - CMS с модулем вакансий
- **Deloitte** - корпоративный job board
- **SmartRecruiters** - HR платформа
- **Ashby** - современная ATS
- **Breezy HR** - платформа для найма
- **BambooHR** - HR и ATS система
- **Factorial** - HR платформа

### Поисковые платформы

- **HeadHunter (hh.ru)** - Россия
- **StepStone.de** - Германия
- **Karriere.at** - Австрия

## Поддерживаемые LLM провайдеры

| Провайдер | Статус | Модели |
|-----------|--------|--------|
| OpenRouter | ✅ По умолчанию | openai/gpt-4o-mini и [300+ других](https://openrouter.ai/models) |
| Ollama | ✅ Готов | gpt-oss:20b, llama3.2, mistral и др. |
| OpenAI | 🔜 Скоро | gpt-4o-mini, gpt-4o |
| Claude | 🔜 Скоро | claude-3-haiku, claude-3-sonnet |

### OpenRouter Provider Routing

OpenRouter поддерживает [Provider Routing](https://openrouter.ai/docs/features/provider-routing) для повышения стабильности:

```bash
# Через CLI
python main.py website https://example.com --openrouter-provider azure

# Через .env файл
OPENROUTER_PROVIDER=azure                    # Конкретный провайдер
OPENROUTER_PROVIDER_ORDER=azure,openai       # Порядок приоритета
OPENROUTER_REQUIRE_PARAMETERS=json_schema    # Требовать structured output
OPENROUTER_ALLOW_FALLBACKS=true              # Разрешить fallback
```

Популярные провайдеры: `azure`, `openai`, `google`, `anthropic`, `deepinfra`, `together`

## Кэширование и история

При поиске на сайтах компаний, вакансии автоматически кэшируются в SQLite базу данных (`data/jobs.db`). 

### Возможности:
- ✅ **Отслеживание новых вакансий** - показывает какие вакансии появились с прошлого сканирования
- ❌ **Отслеживание закрытых вакансий** - показывает какие вакансии больше не доступны
- ↻ **Вернувшиеся вакансии** - если закрытая вакансия снова появилась
- 📜 **История изменений** - команда `history` для просмотра всех изменений

### LLM кэширование

LLM ответы кэшируются с разными TTL для экономии токенов:
- **Вакансии** - 6 часов (часто обновляются)
- **Переводы** - 30 дней (стабильны)
- **URL discovery** - 7 дней (career URLs редко меняются)
- **Информация о компании** - 30 дней (стабильна)

### Отключение кэширования

Если нужен одноразовый поиск без сохранения в БД:
```bash
python main.py website https://example.com --nodb
```

## Гибридная экстракция

Система использует гибридный подход для извлечения вакансий:

1. **Schema.org** (100% точность) - если сайт использует структурированные данные JobPosting
2. **LLM экстракция** - для остальных сайтов используется GPT-модель для анализа HTML

Это позволяет избежать ложных срабатываний от эвристических методов.

### Пагинация

LLM автоматически определяет наличие пагинации на странице и извлекает вакансии с нескольких страниц (по умолчанию до 3 страниц). Если есть ещё страницы, выводится предупреждение.

## Разработка

```bash
# Форматирование кода
black .

# Линтинг
ruff check .

# Тесты
pytest
```

## Лицензия

MIT
